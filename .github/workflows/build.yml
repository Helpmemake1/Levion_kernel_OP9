name: Build NetHunter Kernel

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout Source
        uses: actions/checkout@v4
        with:
          ref: nethunterv2

      - name: Set up Build Environment
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential bc bison flex libssl-dev libncurses-dev libelf-dev git zip gcc-aarch64-linux-gnu erofs-utils e2fsprogs

      - name: Download Clang Toolchain
        run: |
          git clone --depth=1 https://gitlab.com/Panchajanya1999/azure-clang.git "$HOME/clang"

      - name: Restore CAN ISO-TP Module
        run: |
          rm -rf drivers/net/can/can-isotp
          mkdir -p drivers/net/can/can-isotp
          git clone --depth=1 https://github.com/hartkopp/can-isotp.git drivers/net/can/can-isotp
          if [ ! -f "drivers/net/can/can-isotp/Kconfig" ]; then
            echo 'config CAN_ISOTP' > drivers/net/can/can-isotp/Kconfig
            echo '  tristate "ISO 15765-2 CAN transport protocol"' >> drivers/net/can/can-isotp/Kconfig
            echo '  depends on CAN' >> drivers/net/can/can-isotp/Kconfig
            echo '  help' >> drivers/net/can/can-isotp/Kconfig
            echo '    CAN ISO 15765-2 Transport Protocol (ISO-TP)' >> drivers/net/can/can-isotp/Kconfig
          fi

      - name: Setup Defconfig & Built-in Driver Configurations
        run: |
          mkdir -p out
          export PATH="$HOME/clang/bin:$PATH"
          
          # 1. Disable BBG
          sed -i 's/CONFIG_BBG=y/CONFIG_BBG=n/g' arch/arm64/configs/vendor/lahaina-gki_defconfig || true
          
          # 2. Generate base defconfig
          make O=out ARCH=arm64 CC=clang LLVM=1 LLVM_IAS=1 vendor/lahaina-gki_defconfig
          
          # 3. Enable MT76 / MT7610U directly in-kernel (=y) to eliminate vendor_dlkm dependency
          scripts/config --file out/.config --enable CONFIG_NET_MEDIATEK_SOC_WED
          scripts/config --file out/.config --enable CONFIG_MT76_CORE
          scripts/config --file out/.config --enable CONFIG_MT76_LEDS
          scripts/config --file out/.config --enable CONFIG_MT76_USB
          scripts/config --file out/.config --enable CONFIG_MT76x0U
          scripts/config --file out/.config --enable CONFIG_MT76x0_COMMON
          scripts/config --file out/.config --enable CONFIG_MT76x02_LIB
          scripts/config --file out/.config --enable CONFIG_MT76x02_USB
          
          # 4. Explicitly disable BBG and KernelSU
          scripts/config --file out/.config --disable CONFIG_BBG
          scripts/config --file out/.config --disable CONFIG_KSU
          
          # 5. Finalize configuration
          make O=out ARCH=arm64 CC=clang LLVM=1 LLVM_IAS=1 olddefconfig

      - name: Compile Kernel
        run: |
          export PATH="$HOME/clang/bin:$PATH"
          export ARCH=arm64
          export SUBARCH=arm64
          export CC=clang
          
          make -j$(nproc) O=out ARCH=arm64 CC=clang LLVM=1 LLVM_IAS=1 KCFLAGS="-Wno-declaration-after-statement -Wno-error" KBUILD_MODPOST_WARN=1

      - name: Package Standard AnyKernel3 Zip
        run: |
          export PATH="$HOME/clang/bin:$PATH"
          git clone https://github.com/osm0sis/AnyKernel3.git AnyKernel
          
          rm -rf AnyKernel/ramdisk AnyKernel/patch AnyKernel/.git
          
          # 1. Write standard AnyKernel installer script targeting boot & vendor_boot only
          {
            echo "### AnyKernel3 Ramdisk Mod Script"
            echo "properties() { '"
            echo "kernel.string=### Levion Kernel for OnePlus 9/Pro ###"
            echo "do.devicecheck=1"
            echo "do.modules=0"
            echo "do.systemless=1"
            echo "do.cleanup=1"
            echo "do.cleanuponabort=0"
            echo "device.name1=OnePlus9Pro"
            echo "device.name2=lemonadep"
            echo "device.name3=OnePlus9ProTMO"
            echo "device.name4=lemonadept"
            echo "device.name5=OnePlus9"
            echo "device.name6=lemonade"
            echo "device.name7=OnePlus9TMO"
            echo "device.name8=lemonadet"
            echo "device.name9=OnePlus9VZW"
            echo "device.name10=lemonadev"
            echo "supported.versions=15-16"
            echo "'; }"
            echo "fstrim -v /data;"
            echo 'boot_attributes() { set_perm_recursive 0 0 755 644 $RAMDISK/overlay.d/*; set_perm_recursive 0 0 750 750 $RAMDISK/overlay.d/sbin; }'
            echo "BLOCK=boot; IS_SLOT_DEVICE=1; RAMDISK_COMPRESSION=auto; PATCH_VBMETA_FLAG=auto; NO_VBMETA_PARTITION_PATCH=1;"
            echo ". tools/ak3-core.sh;"
            echo "dump_boot; write_boot;"
            echo 'vendor_boot_attributes() { set_perm_recursive 0 0 755 644 $RAMDISK/*; }'
            echo "BLOCK=vendor_boot; IS_SLOT_DEVICE=1; RAMDISK_COMPRESSION=auto; PATCH_VBMETA_FLAG=auto; NO_VBMETA_PARTITION_PATCH=1;"
            echo "reset_ak; dump_boot; write_boot;"
          } > AnyKernel/anykernel.sh

          # 2. Copy vendor_ramdisk, banner, and README if present
          if [ -d "vendor_ramdisk" ]; then cp -r vendor_ramdisk AnyKernel/; fi
          if [ -f "banner" ]; then cp banner AnyKernel/; fi
          if [ -f "README.md" ]; then cp README.md AnyKernel/; fi

          # 3. Copy matched built-in kernel Image
          cp out/arch/arm64/boot/Image AnyKernel/Image

      - name: Upload Flashable Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Levion-NetHunter-MT7610U
          path: AnyKernel/
          
